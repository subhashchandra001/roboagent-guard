# Three-Minute Demo

0:00-0:25: Explain invisible robot risk: localization failures, collision risk, and private data collection.

0:25-0:50: Show architecture: planner -> RoboAgent Guard -> component agents -> supervisor -> digital twin.

0:50-1:15: Run `normal_navigation`; show `approve`.

1:15-1:45: Run `low_light_high_speed`; show `modify` and `relocalize`.

1:45-2:15: Run `unauthorized_camera_request`; show `block`.

2:15-2:40: Run `combined_safety_privacy_crisis`; show physical, SLAM, privacy, and authorization violations.

2:40-3:00: Show `SKILL.md`, `/health`, and `scripts/run_judge_test.py --local`.
